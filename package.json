{
  "name": "pixelrooms-reviews",
  "version": "1.0.0",
  "private": true,
  "description": "SEO-focused reviews site for Pixel Rooms. Static, auto-synced daily.",
  "scripts": {
    "sync": "node scripts/sync.js",
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && npx serve dist"
  },
  "engines": { "node": ">=18" }
}
